# @package _global_

model:
  model: 
    backbone_name: mobilevit_xxs

paths:
  data_cropped_dir: ${paths.data_dir}/cropped_sub/
  frame_skip: 3 # for 5fps

task_name: phd_holodetectorfinal_5fps_s${seed}_${model.model.backbone_name}_${paths.split_name}
