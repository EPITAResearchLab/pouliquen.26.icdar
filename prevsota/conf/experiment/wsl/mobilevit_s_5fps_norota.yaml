# @package _global_

model:
  model: 
    backbone_name: mobilevit_xxs

paths:
  frame_skip: 3 # for 5fps

training:
  trainer:
    epochs_max: 15
    lr: 0.0001
  datamodule:
    flip_rot: False

task_name: phd_wslff_5fps_norota_s${seed}_${model.model.backbone_name}_${paths.split_name}